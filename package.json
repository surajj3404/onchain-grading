{
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.8.1",
    "fs-extra": "^11.1.1",
    "solc": "0.8.8"
  },
  "scripts": {
    "compile": "yarn solcjs --abi --bin --include-path node_modules/ --base-path . -o ."
  }
}
